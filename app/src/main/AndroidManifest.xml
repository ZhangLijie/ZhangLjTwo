<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
package="com.zxc.zhanglj.zhangljtwo">
<application
    android:name=".application.ZljApplication"
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/AppTheme">
    <activity android:name="com.zxc.zhanglj.zhangljtwo.SplashActivity">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>
    <!--APP的环境配置，对应AppConstants的APP_DEBUG和APP_DEV-->
    <meta-data
        android:name="APP_ENV_DEBUG"
        android:value="${app_env_debug}" />

    <meta-data
        android:name="APP_ENV_DEV"
        android:value="${app_env_dev}" />
</application>

</manifest>